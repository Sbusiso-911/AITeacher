<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Pressed State -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/theme_primary_light"
                android:endColor="@color/theme_primary"
                android:angle="135"/>
            <corners android:radius="16dp"/>
            <stroke 
                android:width="1dp" 
                android:color="@color/theme_accent_dark"/>
        </shape>
    </item>
    
    <!-- Normal State -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/theme_surface_variant"
                android:endColor="@color/theme_surface_light"
                android:angle="135"/>
            <corners android:radius="16dp"/>
            <stroke 
                android:width="1dp" 
                android:color="@color/glass_border"/>
        </shape>
    </item>
    
</selector>