<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="@drawable/glassmorphism_input_background">

    <!-- Audio Mode Toggle -->
    <ToggleButton
        android:id="@+id/toggle_audio_mode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textOff="Audio: Off"
        android:textOn="Audio: On"
        android:textColor="@color/glass_text_primary"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/glassmorphism_button_background"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:checked="false"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:contentDescription="Toggle audio interaction mode" />

    <!-- Model Selection -->
    <TextView
        android:id="@+id/label_audio_model"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Audio Model:"
        android:textColor="@color/glass_text_secondary"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/toggle_audio_mode"
        app:layout_constraintStart_toStartOf="parent" />

    <Spinner
        android:id="@+id/spinner_audio_model"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/glassmorphism_button_background"
        android:paddingHorizontal="12dp"
        app:layout_constraintTop_toBottomOf="@id/label_audio_model"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="Select AI model with audio capabilities" />

    <!-- Voice Selection -->
    <TextView
        android:id="@+id/label_voice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Voice:"
        android:textColor="@color/glass_text_secondary"
        android:textSize="12sp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/spinner_audio_model"
        app:layout_constraintStart_toStartOf="parent" />

    <Spinner
        android:id="@+id/spinner_voice"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/glassmorphism_button_background"
        android:paddingHorizontal="12dp"
        app:layout_constraintTop_toBottomOf="@id/label_voice"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="Select voice for audio output" />

    <!-- Audio Controls Container -->
    <LinearLayout
        android:id="@+id/audio_controls_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="24dp"
        android:padding="16dp"
        android:background="@drawable/glassmorphism_button_background"
        app:layout_constraintTop_toBottomOf="@id/spinner_voice"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Record Button -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="24dp">

            <ImageButton
                android:id="@+id/btn_record"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:src="@drawable/ic_mic"
                android:background="@drawable/glassmorphism_circle_button"
                android:scaleType="centerInside"
                android:contentDescription="Hold to record audio message"
                android:elevation="4dp" />

            <!-- Recording Indicator -->
            <View
                android:id="@+id/recording_indicator"
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:layout_gravity="center"
                android:background="@drawable/glassmorphism_circle_button"
                android:visibility="gone"
                android:alpha="0.3" />

        </FrameLayout>

        <!-- Playback Button -->
        <ImageButton
            android:id="@+id/btn_playback"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/ic_send"
            android:background="@drawable/glassmorphism_circle_button"
            android:scaleType="centerInside"
            android:contentDescription="Play recorded audio"
            android:layout_marginStart="16dp"
            android:elevation="4dp" />

    </LinearLayout>

    <!-- Audio Level Visualization -->
    <LinearLayout
        android:id="@+id/audio_level_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="16dp"
        android:padding="12dp"
        android:background="@drawable/glassmorphism_button_background"
        app:layout_constraintTop_toBottomOf="@id/audio_controls_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Audio Level"
            android:textColor="@color/glass_text_secondary"
            android:textSize="10sp"
            android:fontFamily="sans-serif-medium"
            android:layout_gravity="center_horizontal" />

        <ProgressBar
            android:id="@+id/audio_level"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="8dp"
            android:layout_marginTop="4dp"
            android:max="100"
            android:progress="0"
            android:progressTint="@color/trust_blue"
            android:progressBackgroundTint="@color/glass_background" />

    </LinearLayout>

    <!-- Status Text -->
    <TextView
        android:id="@+id/status_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Ready for audio interaction"
        android:textColor="@color/glass_text_secondary"
        android:textSize="12sp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:layout_marginTop="12dp"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/audio_level_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>